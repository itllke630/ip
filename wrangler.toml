# wrangler.toml — Cloudflare Pages project
# Vars are pushed to Cloudflare via `wrangler pages env set` in deploy.yml

name = "ip-linkozen"
compatibility_date = "2026-06-03"
pages_build_output_dir = "."
keep_vars = true
